# -*- mode: snippet -*-
# name: Switch (case) statement
# key: m_switch
# group: Bash Conditionals
# --
case "${1:variable}" in
    ${2:pattern})
        ${3:# code}
        ;;
    ${4:pattern})
        ${5:# code}
        ;;
    *)
        ${0:# default code}
        ;;
esac
