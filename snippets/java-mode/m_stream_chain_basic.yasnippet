# [[file:../../all-snippets.org::*Stream Chaining Snippets][Stream Chaining Snippets:1]]
# -*- mode: snippet -*-
# name: Java Stream Basic Chain
# key: m_stream_chain_basic
# group: Java Streams Chains
# type: snippet
# --
${1:items}.stream()
   .filter(${2:item} -> ${3:$2.isValid()})
   .map(${4:item} -> ${5:$4.transform()})
   .collect(Collectors.toList())${0}
# Stream Chaining Snippets:1 ends here
