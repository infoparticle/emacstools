# -*- mode: snippet -*-
# name: Rust Arc (Atomic Reference Counted)
# key: m_arc
# group: Rust Memory
# type: snippet
# --
use std::sync::Arc;

let ${1:shared} = Arc::new(${2:value});
let ${3:clone} = Arc::clone(&$1);${0:}
